.DS_Store
.plans/
.cal-industries/
.worktrees/
.claude/worktrees/
# Manual-smoke working dir (scripts/smoke.sh): generated dev secrets, fresh DB, tunnel/server
# logs, dev binaries — never committed (the env file holds JWT_SECRET/TOKEN_SECRET).
.smoke/

# Go
/guestpass
/devsmoke
*.test
*.out
coverage.*

# Frontend build output — esbuild → go:embed at release (AD-7), never committed
/web/dist/

# Local SQLite state
*.db
*.db-wal
*.db-shm
